API 开发任务

[NGTP-T202612257]-优化低延时API

svn commit -m  "[NGTP-T202612257] API CMakeLists.txt 优化完成"

学习任务:
    当前 trunk/NewAPI 是一套新的API 框架代码，面向客户，对接三个柜台。 
    study 文件夹下，有几份文档，是对当前代码的分析学习文档，以及简单的设计方案文档。
    特别是 counter.md 和 question.md ，对api 当前框架的进行了深入分析，并且对待完成的任务也进行了相关的分析。
    仔细学习，理解代码，深入掌握整个代码的逻辑，以及设计思路，以及当前需要完成的任务。

知识库任务:
    创建知识库:
        1. 为当前学习的内容 创建专门的知识库文档，整理所学的所有内容。
        2. 创建对应的 Prompt.md 用于引导大模型对于整个知识库的分析和理解。

    更新知识库:
        1. 总结当前所学的， 所总结的所有文档内容，然后更新到知识库中，并且更新 Prompt.md 的内容。

任务 - 优化CMakeLists.txt：    
    1. 初始化任务:
        以 /home/lsz/code/work/grc_trunk/CMakeLists.txt 为模板，优化整个系统的CMakeLists.txt
        不过与 grc_trunk 不同，当前的代码，不怎么依赖外部的库。所以需要减少这部分的内容。
        将当前的 CMakeLists.txt 备份为 CMakeLists.txt.bak
        在当前系统的CMakeLists.txt 开发完成后，进行编译，若是有错误进行相关的修改，直到编译通过。
        若是碰到代码的编译错误，则停止编译，修改代码，直到编译通过。
        将整个修改调整过程遇到的问题，实现的解决方案，和解决的结果，记录到 task/api_dev/cmake_update.md 中。

    2. 调整任务:
        当前 common, gone/api, solarflare ，经过修改后， 现在不支持独立的编译，很多参数要依赖父模块。
        这样不行，参考 /home/lsz/code/work/grc_trunk/DYS-FRAMEWORK/usg/CMakeLists.txt 以及其 父模块的写法，
        使其即能够独立编译，也能够通过父模块编译。

    3. 还有一个需要调整的地方
        在 gone/api 的CMakeLists.txt 中，需要增加 独立编译时，对 SolarFlare 检查与可选编译 的判断和支持。
        这样在独立编译 gone/api 时，也能正确的处理 SolarFlare 相关内容。

任务 - 接口完善;
    任务背景:
        1.当前api框架中，在消息请求 和 消息回报中，只实现了 加速消息相关的接口。这些消息的类型和结构体定义在了 order_trade_type.h 中。
        2.现在将非加速业务的请求和回报的消息结构体存储在了 trunk/NewAPI/gone/api/include/struct_req.h 和 trunk/NewAPI/gone/api/include/struct_ans.h 中。
        3. 
    1. trunk/NewAPI/gone/api/include/struct_req.h 和 trunk/NewAPI/gone/api/include/struct_ans.h 中，
