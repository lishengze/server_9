# non_fast_msg_head.h 文档

> 源文件：`trunk/NewAPI/gone/api/include/non_fast_msg_head.h`
> 状态：**空文件（0 字节）**，当前无任何内容。

## 概述

本文件当前为空，但根据文件名和项目结构推断，其设计意图是定义**非加速消息的消息头**。

## 预期用途

- **非加速接口**：指不走极速柜台（fpga/gw）的低延迟通道、而是走普通通道的消息（如 98 柜台的查询类消息）。
- **消息头**：非加速消息在传输时需要一个统一的消息头（包含消息 ID、消息长度、序号等），本文件预计定义该消息头结构体。

## 当前状态

- 文件已创建但内容为空，属于**待实现**部分。
- 需要结合 `struct_req.h` / `struct_ans.h`（非加速接口的请求/应答结构体）和 98 柜台协议（`c98msg_tmp.h`）来确定消息头定义。

## 关联

- `c98msg_tmp.h`：98 柜台临时消息头定义（`c98_msg_head_tmp`）
- `struct_req.h` / `struct_ans.h`：非加速接口的请求/应答消息体
- `question.md` 第六章 6.4.4：非加速消息接口待补充

## 设计要点
1. 该文件是任务文档中"非加速消息接口"的一部分，当前尚未实现。
2. 定义消息头时需要与 `c98_msg_head_tmp` 保持一致的字节序和布局，确保与 98 柜台兼容。
3. 待协议明确后填充内容。